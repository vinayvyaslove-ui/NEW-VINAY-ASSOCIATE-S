export default function AccountantPage() {
  return (
    <main className="min-h-screen p-8">
      <h2 className="text-2xl font-semibold mb-4">Accountant Dashboard (stub)</h2>
      <p>Access control: use Supabase RLS and 'role' checks in server endpoints to grant accountant access.</p>
    </main>
  )
}
